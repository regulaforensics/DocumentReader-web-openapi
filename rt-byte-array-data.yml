components:
  schemas:
    ByteArrayResult:
      title: "ByteArrayResult"
      allOf:
        - type: object
          required:
            - ByteArray
          properties:
            ByteArray:
              description: "Byte array in base64"
              type: string
        - $ref: "./rt.yml#/components/schemas/ResultItem"
