openapi: 3.0.3
components:
  schemas:
    ByteArrayResult:
      title: "ByteArray"
      allOf:
        - type: object
          required:
            - ByteArray
          properties:
            ByteArray:
              description: "Byte array in base64"
              type: string
        - $ref: "./rt.yml#/components/schemas/ResultItem"
